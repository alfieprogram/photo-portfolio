<!-- views/index.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dayln Portfolio - Home</title>
  <link rel="stylesheet" href="/styles/main.css">
  <link rel="stylesheet" href="/styles/index.css">
</head>
<body>
  <header>

    <nav>
      <ul>
     
        <li><a href="/about">About</a></li>
        <li><a href="mailto:contact@photo.daydev.lol"">Contact</a></li>
        <li><a href="/album">Album</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <div id="popup-container">
      <div id="popup-content">
          <span id="close-btn" onclick="closePopup()">&times;</span>
          <h2>New Album Alert!</h2>
          <p>We've just dropped a new Photos!. Check it out now!</p>
          <a href="/album">View Now!</a>
      </div>
  </div>
    <div class="auto-type-container">
      <h1 id="auto-type-text"></h1>
    </div>
  
    <script>
      const texts = ["Welcome to my Photo Portfolio!", "I am a Photographer that use Iphone 11 and Nikon D3100", "Time for your photo 📸"]; // Replace with your array of h1 elements
      let index = 0;
      const textElement = document.getElementById("auto-type-text");
  
      function autoType() {
        if (index < texts.length) {
          const currentText = texts[index];
          const currentLength = currentText.length;
  
          textElement.textContent = currentText.substring(0, currentLength);
          index++;
  
          setTimeout(autoType, 10000); // Adjust the delay (in milliseconds) between each character
        } else {
          // Reset index for continuous looping
          index = 0;
          setTimeout(autoType, 1000); // Adjust the delay before restarting (in milliseconds)
        }
      }
  
      // Start the auto-typing when the page loads
      window.onload = autoType;
    </script>
  </main>
  <footer>
    <p>&copy; 2024 Day Portfolio</p>
  </footer>

<script>
  document.addEventListener("DOMContentLoaded", function() {
    // Show the popup when the page loads
    setTimeout(function() {
        document.getElementById("popup-container").style.display = "block";
    }, 3000); // Show after 3 seconds (adjust as needed)
});

function closePopup() {
    // Close the popup when the close button is clicked
    document.getElementById("popup-container").style.display = "none";
}

</script>
</body>
</html>
